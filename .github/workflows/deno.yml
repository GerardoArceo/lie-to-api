name: Deno

on:
  push:
    branches: [master]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      uses: garygrossgarten/github-action-scp@release
      with:
        local: server
        remote: scp/directory
        host: "34.230.148.142"
        username: "bitnami"
        password: ${{ secrets.LIGHTSAILDEFAULTKEY }}